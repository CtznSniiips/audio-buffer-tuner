# audio-buffer-tuner
Dispatcharr plugin that lowers the initial TS-proxy prebuffer chunk count, and optionally the chunk size itself, for channels in a handful of chosen Channel Groups. Intended for steady, low-bitrate audio-only streams.
